# Default local development configuration.
# Override any value via the corresponding environment variable (see config.template).

[ipaws]
endpoint = https://tds.fema.gov/ipaws
cert_path = /certs/dummy.pem
sandbox = true
poll_interval_seconds = 30

[rapid_alert]
url = http://localhost:8080

[mesh_gateway]
url = http://localhost:8090
severity_threshold = severe

[server]
host = 0.0.0.0
port = 8000
